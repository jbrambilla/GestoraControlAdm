@model IEnumerable<EGestora.GestoraControlAdm.Application.ViewModels.AuditControllerViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ControllerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ControllerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AuditControllerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AuditControllerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AuditControllerId })
        </td>
    </tr>
}

</table>
